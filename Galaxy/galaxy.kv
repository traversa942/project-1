#:import menu menu

MainWidget:

<MainWidget>:
    canvas.before:
        Rectangle:
            size: (self.width, self.width / self.bg_image_ratio) if self.width / self.height < self.bg_image_ratio else (self.height * self.bg_image_ratio, self.height)
            pos: (self.center_x - self.width / 2, self.center_y - self.height / 2)
            source:'C:\\Users\\g202393750\\Documents\\python codes\\Galaxy\\images\\bg1.jpg'
    menu_widget: menu_widget
    perspective_point_x: self.width/2
    perspective_point_y: self.height*0.75
    Label:
        text: root.score_txt
        font_size: dp(20)
        font_name: 'C:\\Users\\g202393750\\Documents\\python codes\\Galaxy\\fonts\\Eurostile.ttf'
        size_hint: .2,.2
        pos_hint: {"x": 0, "top": 1}
    MenuWidget:
        id: menu_widget


    
    